name: Run aengbot

on:
  schedule:
    - cron: '22 13 12 * *'
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: |
          echo "Hello, world!"
    